# spring.application.name=mlm
# server.port=8080
# PostgreSQL Configuration
spring.datasource.url=${DB_STRING}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver


# PostgreSQL Configuration
# spring.datasource.url=jdbc:postgresql://aws-postgresql.cst8y4iucesv.us-east-1.rds.amazonaws.com:5432/postgres?currentSchema=mlm_schema&sslmode=require
# spring.datasource.username=mlm
# spring.datasource.password=x*76$]LKjkdnn2h$32
# spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JMX Actuator Endpoints for Dev environment
# spring.jmx.enabled=true
# management.endpoints.jmx.exposure.include=*
